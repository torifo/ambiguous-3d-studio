/**
 * Placeholder shell (Wave 1). Task 4.1 owns the real composition:
 * sidebar frame + viewport frame + useGenerationPipeline wiring.
 */
export default function App() {
  return (
    <div className="flex h-screen items-center justify-center bg-neutral-950 text-neutral-100">
      <p className="text-sm tracking-wide">Ambiguous 3D Studio — scaffold</p>
    </div>
  )
}
