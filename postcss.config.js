// Tailwind CSS v4 — wired through PostCSS so vite.config.ts stays exactly
// the mandated shape (plugins/base/worker) from design.md "Deployment".
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
